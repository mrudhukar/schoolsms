<% @breadcrumbs = [link_to('All Students', students_path)] %>
<% @title = @is_edit_class ? "Update Class Details" : "Edit Student" %>

<% if @is_edit_class %>
  <%= render 'new_student_year' %>
<% else %>
  <%= render 'form' %>
<% end %>